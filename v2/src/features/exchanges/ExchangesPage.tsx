import { Empty, Typography } from 'antd';

export function ExchangesPage() {
  return (
    <div>
      <Typography.Title level={3}>Exchanges</Typography.Title>
      <Empty description="Exchanges view — coming in a later phase" style={{ marginTop: 80 }} />
    </div>
  );
}
